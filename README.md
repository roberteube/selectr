# Simple PySide6 File Manager

This is a minimal file manager built with PySide6.

Run:

```bash
python main.py
```

Install dependencies:

```bash
pip install -r requirements.txt
```

Features:
- Directory tree on the left
- File list on the right
- Open, Refresh, New Folder, Delete actions
- Enter a path in the address bar and press Enter
